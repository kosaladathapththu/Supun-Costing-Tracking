import {useState} from 'react';import {Plus,Package} from 'lucide-react';import {useApp} from '../context/AppContext';import {Badge,Button,Field,Modal,PageHeader,SearchBox} from '../components/UI';import {uid} from '../utils/calculations';
export default function Products(){const {data,update}=useApp();const [q,setQ]=useState('');const [modal,setModal]=useState(false);const blank={code:'',name:'',category:data.categories[0],type:'Finished Product',weight:'',volume:'',status:'Active'};const [form,setForm]=useState(blank);const list=data.products.filter(p=>(p.name+p.code+p.category).toLowerCase().includes(q.toLowerCase()));const save=e=>{e.preventDefault();update('products',p=>[...p,{...form,id:uid('PRD')}]);setModal(false);setForm(blank)};return <><PageHeader eyebrow="MASTER DATA" title="Products" description="Manage the products used in shipment costings." action={<Button onClick={()=>setModal(true)}><Plus size={18}/>Add product</Button>}/><div className="card"><div className="toolbar"><SearchBox value={q} onChange={setQ} placeholder="Search products..."/><span>{list.length} products</span></div><div className="table-wrap"><table><thead><tr><th>Product</th><th>Category</th><th>Type</th><th>Weight</th><th>Volume</th><th>Status</th></tr></thead><tbody>{list.map(p=><tr key={p.id}><td><div className="product-cell"><div className="mini-icon"><Package/></div><span><b>{p.name}</b><small>{p.code}</small></span></div></td><td>{p.category}</td><td>{p.type}</td><td>{p.weight} kg</td><td>{p.volume} m³</td><td><Badge>{p.status}</Badge></td></tr>)}</tbody></table></div></div>{modal&&<Modal title="Add product" onClose={()=>setModal(false)}><form onSubmit={save} className="form-grid"><Field label="Product code"><input required value={form.code} onChange={e=>setForm({...form,code:e.target.value})}/></Field><Field label="Product name"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="Category"><select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{data.categories.map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Product type"><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}><option>Finished Product</option><option>Component</option><option>Spare Part</option><option>Accessory</option></select></Field><Field label="Weight (kg)"><input type="number" step="any" value={form.weight} onChange={e=>setForm({...form,weight:e.target.value})}/></Field><Field label="Volume (m³)"><input type="number" step="any" value={form.volume} onChange={e=>setForm({...form,volume:e.target.value})}/></Field><div className="form-actions"><Button type="button" variant="secondary" onClick={()=>setModal(false)}>Cancel</Button><Button>Save product</Button></div></form></Modal>}</>}
