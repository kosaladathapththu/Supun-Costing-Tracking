import {useState} from 'react';
import {LockKeyhole,ArrowRight,ShieldCheck} from 'lucide-react';
import {useApp} from '../context/AppContext';
export default function Login(){const {login}=useApp();const [email,setEmail]=useState('cfo@supungroup.lk');const [password,setPassword]=useState('demo123');const [error,setError]=useState('');const submit=e=>{e.preventDefault();if(!email||!password)return setError('Enter your email and password.');login(email)};return <div className="login-page"><div className="login-brand"><div className="brand-mark large">SG</div><h1>Know the true cost.<br/><em>Price with confidence.</em></h1><p>A single source of truth for landed costs, pricing and profitability across Supun Group.</p><div className="secure-note"><ShieldCheck/><span><b>Secure financial workspace</b><small>Role-based access and full audit history</small></span></div></div><form className="login-card" onSubmit={submit}><div className="mobile-logo"><div className="brand-mark">SG</div><b>SUPUN GROUP</b></div><span className="kicker">WELCOME BACK</span><h2>Sign in to your account</h2><p>Use your company credentials to continue.</p><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} /></label><label>Password<div className="password"><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/><LockKeyhole size={18}/></div></label>{error&&<div className="error">{error}</div>}<button className="btn primary login-btn">Sign in <ArrowRight size={18}/></button><small className="demo-hint">Demo mode — any email and password will work</small></form></div>}
