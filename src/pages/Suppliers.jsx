import {useState} from 'react';import {Plus,MapPin,Truck} from 'lucide-react';import {useApp} from '../context/AppContext';import {Button,Field,Modal,PageHeader,SearchBox} from '../components/UI';import {uid} from '../utils/calculations';
export default function Suppliers(){const {data,update}=useApp();const [q,setQ]=useState('');const [modal,setModal]=useState(false);const blank={name:'',country:'',currency:'USD',contact:'',notes:''};const [form,setForm]=useState(blank);const list=data.suppliers.filter(s=>(s.name+s.country).toLowerCase().includes(q.toLowerCase()));const save=e=>{e.preventDefault();update('suppliers',s=>[...s,{...form,id:uid('SUP')}]);setModal(false);setForm(blank)};return <><PageHeader eyebrow="MASTER DATA" title="Suppliers" description="Supplier details, currencies and sourcing information." action={<Button onClick={()=>setModal(true)}><Plus size={18}/>Add supplier</Button>}/><div className="toolbar standalone"><SearchBox value={q} onChange={setQ}/><span>{list.length} suppliers</span></div><div className="supplier-grid">{list.map(s=><div className="supplier-card" key={s.id}><div className="supplier-icon"><Truck/></div><div><h3>{s.name}</h3><p><MapPin size={15}/>{s.country}</p></div><span className="currency">{s.currency}</span><dl><div><dt>Contact</dt><dd>{s.contact||'—'}</dd></div><div><dt>Notes</dt><dd>{s.notes||'—'}</dd></div></dl></div>)}</div>{modal&&<Modal title="Add supplier" onClose={()=>setModal(false)}><form onSubmit={save} className="form-grid"><Field label="Supplier name"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="Country"><input required value={form.country} onChange={e=>setForm({...form,country:e.target.value})}/></Field><Field label="Currency"><select value={form.currency} onChange={e=>setForm({...form,currency:e.target.value})}>{['LKR','USD','EUR','GBP','JPY','CNY'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Contact details"><input value={form.contact} onChange={e=>setForm({...form,contact:e.target.value})}/></Field><Field label="Notes"><textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></Field><div className="form-actions"><Button type="button" variant="secondary" onClick={()=>setModal(false)}>Cancel</Button><Button>Save supplier</Button></div></form></Modal>}</>}
