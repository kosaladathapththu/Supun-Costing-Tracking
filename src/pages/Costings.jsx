import {useMemo,useState} from 'react';import {useNavigate} from 'react-router-dom';import {Plus,ChevronRight} from 'lucide-react';import {useApp} from '../context/AppContext';import {Badge,Button,PageHeader,SearchBox} from '../components/UI';import {calculateCosting,money} from '../utils/calculations';
export default function Costings(){const {data}=useApp(),nav=useNavigate();const [q,setQ]=useState('');const list=useMemo(()=>data.costings.map(calculateCosting).filter(c=>(c.reference+c.id).toLowerCase().includes(q.toLowerCase())),[data.costings,q]);return <><PageHeader eyebrow="COST MANAGEMENT" title="Costings" description="Create, review and finalize shipment landed costs." action={<Button onClick={()=>nav('/costings/new')}><Plus size={18}/>New costing</Button>}/><div className="card"><div className="toolbar"><SearchBox value={q} onChange={setQ} placeholder="Search reference or costing ID..."/><div className="filter-pills"><button className="active">All</button><button>Draft</button><button>Under review</button><button>Finalized</button></div></div><div className="table-wrap"><table><thead><tr><th>Costing</th><th>Supplier</th><th>Date</th><th>Items</th><th>Purchase cost</th><th>Landed cost</th><th>Status</th><th></th></tr></thead><tbody>{list.map(c=><tr key={c.id} onClick={()=>nav(`/costings/${c.id}`)}><td><b>{c.reference}</b><small>{c.id}</small></td><td>{data.suppliers.find(s=>s.id===c.supplierId)?.name}</td><td>{c.date}</td><td>{c.items.length}</td><td>{money(c.purchaseTotal)}</td><td><b>{money(c.landedTotal)}</b></td><td><Badge>{c.status}</Badge></td><td><ChevronRight size={17}/></td></tr>)}</tbody></table></div></div></>}
