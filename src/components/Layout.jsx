import {useState} from 'react';
import {NavLink,useLocation} from 'react-router-dom';
import {LayoutDashboard,Package,FileBarChart,Truck,Settings,Menu,LogOut,Bell,ChevronDown} from 'lucide-react';
import {useApp} from '../context/AppContext';
const nav=[['/','Dashboard',LayoutDashboard],['/products','Product Costing',Package],['/reports','Reports',FileBarChart],['/suppliers','Suppliers',Truck],['/settings','Settings',Settings]];
export default function Layout({children}){const [open,setOpen]=useState(false);const {user,logout}=useApp();const location=useLocation();const label=nav.find(n=>n[0]===location.pathname)?.[1]||'Costing';return <div className="shell"><aside className={open?'open':''}><div className="brand"><div className="brand-mark">SG</div><div><b>SUPUN GROUP</b><span>Costing & Pricing</span></div></div><nav>{nav.map(([to,text,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)}><Icon size={19}/><span>{text}</span></NavLink>)}</nav><div className="side-user"><div className="avatar">{user?.name?.[0]?.toUpperCase()}</div><div><b>{user?.name}</b><span>{user?.role}</span></div><button onClick={logout} title="Log out"><LogOut size={17}/></button></div></aside>{open&&<div className="scrim" onClick={()=>setOpen(false)}/>}<main><header><button className="menu" onClick={()=>setOpen(true)}><Menu/></button><div><span className="mobile-title">{label}</span></div><div className="head-actions"><button className="icon-btn"><Bell size={19}/><i/></button><div className="profile"><div className="avatar">{user?.name?.[0]?.toUpperCase()}</div><span>{user?.name}</span><ChevronDown size={15}/></div></div></header><div className="content">{children}</div></main></div>}
